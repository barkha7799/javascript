var loader = {
  functions: [],

  push: function(fn) {
    loader.functions.push(fn);
  },

  exec: function() {
    for (var i = 0, len = loader.functions.length; i < len; i++) {
      loader.functions[i]();
    }

    loader.functions = [];
  }
};
